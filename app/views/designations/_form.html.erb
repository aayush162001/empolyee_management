<%= form_with(model: designation, local: true) do |form| %>
  <% if designation.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2><%= pluralize(designation.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% designation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "Save Designation", class: "btn btn-primary" %>
  </div>
<% end %>
