<!DOCTYPE html>
<html>
<head>
  <title>Holidays#index</title>
  <!-- Add Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
 <%= link_to 'Back', root_path , class: "btn btn-info back-btn" %>
  <h1>Holidays#index</h1>
  <p>Find me in app/views/holidays/index.html.erb</p>

  <table class="table table-bordered table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>Date</th>
        <th colspan="3" , class="text-center" >Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @holidays.each do |holiday| %>
        <tr>
          <td><%= (holiday.title).titlecase %></td>
          <td><%= holiday.holiday_date %></td>
          <td><%= link_to 'Show', holiday, class: 'btn btn-primary' %></td>
          <td><%= link_to 'Edit', edit_holiday_path(holiday), class: 'btn btn-secondary' %></td>
          <td><%= link_to 'Destroy', holiday, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'Add New holiday', new_holiday_path, class: 'btn btn-success' %>

  <!-- Add Bootstrap 5 JS (Optional, only required for certain Bootstrap features) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
