<!DOCTYPE html>
<html>
<head>
  <title>Email Hierarchy Details</title>
  <!-- Include Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1>Email Hierarchy Details</h1>
    <p><strong>User_id : </strong> <%= @email_hierarchy.user_id %> | 
    <%= User.find(@email_hierarchy.user_id)[:email] %></p>
    <p><strong>To User_id : </strong> <%= @email_hierarchy.too %> | 
    <%= User.find(@email_hierarchy.too.split(',')).pluck(:email).join(', ') %></p>
    <p><strong>Cc User_id : </strong> <%= @email_hierarchy.cc %> | 
    <%= User.find(@email_hierarchy.cc.split(',')).pluck(:email).join(', ') %></p>
    <br><br>
    <%= link_to 'Back', email_hierarchy_index_path, class: "btn btn-secondary" %>

    <%= link_to '<i class="fas fa-edit"></i>'.html_safe, edit_email_hierarchy_path(@email_hierarchy), class: 'btn btn-secondary btn-sm' %>
    <%= link_to '<i class="fas fa-trash"></i>'.html_safe, @email_hierarchy, method: :delete, data: {turbo_method: :delete, }, class: 'btn btn-danger btn-sm' %>


  </div>

  <!-- Include Bootstrap JS and dependencies (optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
